services:
  ebs:
    build:
      context: ..
      dockerfile: ebs/Dockerfile
    env_file:
      - ../.env.production
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ebs_data:/app/data

volumes:
  ebs_data:
